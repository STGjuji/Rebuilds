import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'TWitchRebuild',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
